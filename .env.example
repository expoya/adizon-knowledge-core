# =============================================================================
# Adizon Knowledge Core - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
#
# FOR RAILWAY DEPLOYMENT:
# - Set these variables in Railway Dashboard > Service > Variables
# - Railway provides DATABASE_URL automatically for PostgreSQL add-ons
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL + pgvector
# -----------------------------------------------------------------------------
# For local development:
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# For Railway: Use Railway's PostgreSQL add-on, DATABASE_URL is auto-set
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/db

# -----------------------------------------------------------------------------
# Neo4j Graph Database
# -----------------------------------------------------------------------------
NEO4J_PASSWORD=your-secure-password

# For Railway: Deploy Neo4j as separate service or use Aura cloud
# NEO4J_URI=bolt://your-neo4j-host:7687
# NEO4J_USER=neo4j

# -----------------------------------------------------------------------------
# MinIO S3-Compatible Storage
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# For Railway: Consider using Railway's Object Storage or external S3
# MINIO_ENDPOINT=your-minio-host:9000
# MINIO_ACCESS_KEY=your-key
# MINIO_SECRET_KEY=your-secret

# -----------------------------------------------------------------------------
# AI API (REQUIRED - Trooper or OpenAI-compatible endpoint)
# Used for both embeddings and LLM calls
# -----------------------------------------------------------------------------
EMBEDDING_API_URL=https://your-api-server.com/v1
EMBEDDING_API_KEY=your-api-key
EMBEDDING_MODEL=jina/jina-embeddings-v2-base-de

# LLM for graph extraction
LLM_MODEL_NAME=adizon-ministral

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_ENV=production
APP_DEBUG=false

# CORS Origins (comma-separated) - Add your Railway frontend URL
CORS_ORIGINS=https://your-frontend.railway.app,http://localhost:5173

# -----------------------------------------------------------------------------
# Frontend Build (for Docker build)
# -----------------------------------------------------------------------------
# Set this to your Railway backend URL for production builds
VITE_API_URL=https://your-backend.railway.app/api/v1
